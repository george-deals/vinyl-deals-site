name: Refresh 4k-uhd feed (hourly)

on:
  schedule:
    - cron: "20 * * * *"
  workflow_dispatch:

concurrency:
  group: refresh-4k-uhd
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Refresh 4k-uhd
        env:
          BASE_URL: ${{ secrets.PROD_BASE_URL }}
          TOKEN: ${{ secrets.REFRESH_TOKEN }}
        run: |
          set -euo pipefail
          TYPE="4k-uhd"

          BASE="$(echo "${BASE_URL:-}" | tr -d '\r\n' | xargs)"
          BASE="${BASE%/}"
          if [ -z "$BASE" ]; then
            BASE="https://www.mediadealshub.com"
          fi

          echo "Refreshing: $TYPE"
          echo "BASE: $BASE"

          echo "Revalidating active $TYPE deals..."
          REVALIDATE_LIMIT=60
          REVALIDATE_TOTAL=360
          revalidate_offset=0
          revalidate_calls=0
          revalidate_success=0

          while [ $revalidate_offset -lt $REVALIDATE_TOTAL ]; do
            revalidate_calls=$((revalidate_calls + 1))
            revalidate_url="$BASE/api/refresh-$TYPE?token=$TOKEN&revalidateActive=1&revalidateOnly=1&activeIncludeAll=1&activeLimit=${REVALIDATE_LIMIT}&activeOffset=${revalidate_offset}"
            echo "Revalidate offset=$revalidate_offset limit=$REVALIDATE_LIMIT"

            ok=0
            for i in 1 2; do
              set +e
              RESP=$(curl -sS --fail-with-body \
                --connect-timeout 20 \
                --max-time 180 \
                "$revalidate_url")
              CODE=$?
              set -e
              if [ $CODE -eq 0 ]; then
                echo "$RESP"
                ok=1
                revalidate_success=$((revalidate_success + 1))
                break
              fi
              echo "Revalidate call failed (code=$CODE, attempt=$i). Sleeping..."
              sleep $((8 * i))
            done

            if [ $ok -eq 0 ]; then
              echo "WARNING: revalidate chunk failed (offset=$revalidate_offset)."
            fi

            revalidate_offset=$((revalidate_offset + REVALIDATE_LIMIT))
            sleep 1
          done

          if [ $revalidate_success -eq 0 ]; then
            echo "ERROR: all 4k revalidate calls failed"
            exit 1
          fi

          PER_RUN=10
          TOTAL=0
          MAXPAGES=1
          BOOTSTRAP_LIMIT=80
          REQUEST_BUDGET_MS=70000
          fails=0
          offset=0

          while [ $offset -lt $TOTAL ]; do
            URL="$BASE/api/refresh-$TYPE?token=$TOKEN&maxPages=${MAXPAGES}&delayMs=0&moviesPerRun=${PER_RUN}&moviesOffset=${offset}&bootstrapLimit=${BOOTSTRAP_LIMIT}&requestBudgetMs=${REQUEST_BUDGET_MS}&bootstrapReserveMs=25000"
            echo "Chunk offset=$offset size=$PER_RUN"

            ok=0
            for i in 1 2; do
              echo "Attempt $i..."
              set +e
              RESP=$(curl -sS --fail-with-body --max-time 180 "$URL")
              CODE=$?
              set -e
              if [ $CODE -eq 0 ]; then
                echo "$RESP"
                ok=1
                break
              fi
              echo "curl failed (code=$CODE). Sleeping..."
              sleep $((10 * i))
            done

            if [ $ok -eq 0 ]; then
              fails=$((fails + 1))
              echo "Chunk failed after retries (offset=$offset)."
            fi

            offset=$((offset + PER_RUN))
            sleep 2
          done

          if [ $fails -gt 0 ]; then
            echo "Completed with failures: $fails chunk(s) failed."
            exit 1
          fi

          echo "Done."
